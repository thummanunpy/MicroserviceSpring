spring:
  cloud:
    config:
      name: product-service
      discovery:
        enabled: true
        service-id: config-server
  application:
    name: product-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka