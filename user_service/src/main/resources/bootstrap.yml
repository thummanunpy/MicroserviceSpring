spring:
  cloud:
    config:
      name: user-service
      discovery:
        enabled: true
        service-id: config-server
  application:
    name: user-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka