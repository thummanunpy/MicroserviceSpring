spring:
  cloud:
    config:
      name: gateway-service
      discovery:
        enabled: true
        service-id: config-server
  application:
    name: gateway-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka